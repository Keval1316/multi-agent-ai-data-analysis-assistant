# Sample Datasets

This directory contains test datasets:
- `clean_dataset.csv`: Standard clean tabular dataset for baseline testing.
- `messy_dataset.csv`: Deliberately corrupted dataset with nulls, duplicates, outliers, mixed types, and dirty categorical values.
